% macos

# CFLAGS for macOS
export CFLAGS="-I$(brew --prefix <lib>)/include"

# LDFLAGS
export LDFLAGS="-L$(brew --prefix <lib>)/lib"

# Remove quarantine attribute bit from file
xattr -d com.apple.quarantine <filename>

# List all extended attributes
ls -l@
